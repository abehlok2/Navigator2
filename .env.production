# Navigator Backend Production Configuration
# Copy this file to .env and update with your production values

# Secret key for JWT token signing (REQUIRED - CHANGE THIS!)
# Generate a secure random string: openssl rand -hex 32
NAVIGATOR_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING

# Pre-defined users (REQUIRED - registration is disabled)
# Users must be defined as a JSON array with username, password, and optional email/displayName
# Username requirements: at least 3 characters, alphanumeric with hyphens and underscores
# Password requirements: at least 8 characters
# IMPORTANT: Use strong passwords in production!
NAVIGATOR_PRESET_USERS='[{"username":"admin","password":"CHANGE_THIS_PASSWORD","displayName":"Administrator"},{"username":"facilitator1","password":"CHANGE_THIS_PASSWORD","displayName":"Facilitator One"}]'

# Port (optional, defaults to 4000)
PORT=4000

# Frontend environment variables (for docker-compose build args)
# These should match your domain/hosting setup
# For local development: ws://localhost/signaling and http://localhost
# For production with domain: wss://yourdomain.com/signaling and https://yourdomain.com
VITE_SIGNALING_SERVER_URL=ws://localhost/signaling
VITE_SOCKET_SERVER_URL=http://localhost
